@model IEnumerable<CinemaX.Models.Utilizador>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserPassWord)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGrupoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserPassWord)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdGrupoNavigation.NomeGrupo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdUtilizador">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdUtilizador">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdUtilizador">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
